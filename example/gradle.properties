# This build includes the SDK build, so it needs the same JDK discovery: the
# daemon runs on a modern JVM while the toolchain compiles against Java 11.
org.gradle.java.installations.fromEnv=JAVA_HOME_11_X64,JAVA_HOME_21_X64
